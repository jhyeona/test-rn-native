import {
  isLoginState,
  selectDayScheduleDate,
  selectWeekScheduleDate,
} from './atom.ts';

const globalState = {
  isLoginState,
  selectDayScheduleDate,
  selectWeekScheduleDate,
};

export default globalState;
