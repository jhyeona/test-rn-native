import {
  dayScheduleState,
  nowScheduleHistoryState,
  weekScheduleState,
} from '#recoil/Schedule/atom.ts';

const scheduleState = {
  dayScheduleState,
  weekScheduleState,
  nowScheduleHistoryState,
};

export default scheduleState;
