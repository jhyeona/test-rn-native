import {
  dayScheduleState,
  nowScheduleHistoryState,
  weekScheduleState,
} from './atom.ts';

const scheduleState = {
  dayScheduleState,
  weekScheduleState,
  nowScheduleHistoryState,
};

export default scheduleState;
